<?php
/**
 * Local deploy config — copy to deploy.local.php (gitignored).
 *
 * WORKFLOW (no local Node build required):
 *   1. Edit frontend (app/, components/) or PHP (application/)
 *   2. git add . && git commit -m "..." && git push origin main
 *   3. GitHub Actions builds Next.js + rsyncs to Hostinger automatically
 *
 * Optional manual deploy (same as CI, from a machine with Node + SSH):
 *   npm run build:deploy && npm run deploy:rsync
 */
return [
    'ssh_host'  => 'YOUR_HOSTINGER_SSH_HOST',
    'ssh_user'  => 'u221026474',
    'ssh_port'  => 65002,
    'ssh_key'   => 'C:/Users/YOUR_USER/.ssh/deploy_ilf',

    'target_path' => '/home/u221026474/domains/superfinelabels.in/public_html/cp',

    'git_repo'   => 'https://github.com/VELRK/cp.git',
    'git_branch' => 'main',

    'deploy_token' => 'CHANGE_ME',
];
