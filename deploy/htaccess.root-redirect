# cp-panel-redirect — optional site-root rules (appended by GitHub deploy)
# Redirects old /panel URLs to the app under /cp/
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^panel/?(.*)$ /cp/panel/$1 [R=302,L,QSA]
</IfModule>
